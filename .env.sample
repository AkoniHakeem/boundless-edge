# EmailJS Configuration - Sample File
# Copy this file to .env and replace with your actual EmailJS credentials
# Get these values from https://www.emailjs.com/

EMAILJS_SERVICE_ID=your_service_id_here
EMAILJS_TEMPLATE_ID=your_template_id_here
EMAILJS_PUBLIC_KEY=your_public_key_here

# Instructions:
# 1. Create an account at https://www.emailjs.com/
# 2. Set up an email service (Gmail, Outlook, etc.)
# 3. Create an email template using the structure in docs/EMAILJS_SETUP.md
# 4. Replace the placeholder values above with your actual credentials
# 5. Rename this file to .env (remove .sample)

# Security Note:
# The .env file should never be committed to version control
# It's already listed in .gitignore to keep your credentials secure
